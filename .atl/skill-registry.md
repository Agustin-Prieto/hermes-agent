# Skill Registry

> Auto-generated by sdd-init on 2026-05-29.
> Prefer project-level skills over user-level skills. Skip `sdd-*`, `_shared`, and `skill-registry`.

## branch-pr
- **Trigger**: When creating a pull request, opening a PR, or preparing changes for review.
- **Path**: `/Users/agustinprieto/.claude/skills/branch-pr/SKILL.md`
- **Compact Rules**:
  - Every PR MUST link an approved issue (`status:approved`).
  - Every PR MUST have exactly one `type:*` label.
  - Branch names MUST match `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`.
  - Commit messages MUST match conventional commits regex: `^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\([a-z0-9\._-]+\))?!?: .+`.
  - Run `shellcheck` on modified scripts before pushing.
  - Use `Closes #N`, `Fixes #N`, or `Resolves #N` in PR body.

## chained-pr
- **Trigger**: PRs over 400 lines, stacked PRs, review slices.
- **Path**: `/Users/agustinprieto/.config/opencode/skills/chained-pr/SKILL.md`
- **Compact Rules**:
  - Split PRs over 400 changed lines unless a maintainer accepts `size:exception`.
  - Keep each PR reviewable in ≤60 minutes; one deliverable work unit per PR.
  - State start, end, prior dependencies, follow-up work, and out-of-scope items.
  - Every child PR must include a dependency diagram marking the current PR with `📍`.
  - In Feature Branch Chain, create a draft/no-merge tracker PR; child PR #1 targets the tracker branch.
  - Treat polluted diffs as base bugs: retarget or rebase until only the current work unit appears.
  - Do not mix chain strategies after the user chooses one.

## cognitive-doc-design
- **Trigger**: Writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs.
- **Path**: `/Users/agustinprieto/.config/opencode/skills/cognitive-doc-design/SKILL.md`
- **Compact Rules**:
  - Lead with the answer; context comes after.
  - Use progressive disclosure: happy path first, then details and edge cases.
  - Chunk related information into small sections; keep flat lists short.
  - Use signposting (headings, labels, callouts, summaries) so readers know where they are.
  - Prefer tables, checklists, and examples over prose that must be remembered.
  - Design docs so reviewers can verify intent without reconstructing the whole story.
  - Default doc shape: Outcome title → One paragraph → Quick path → Details table → Checklist → Next step.

## comment-writer
- **Trigger**: PR feedback, issue replies, reviews, Slack messages, or GitHub comments.
- **Path**: `/Users/agustinprieto/.config/opencode/skills/comment-writer/SKILL.md`
- **Compact Rules**:
  - Start with the actionable point; do not recap the whole PR before feedback.
  - Be warm and direct like a thoughtful teammate, not a corporate bot.
  - Prefer 1 to 3 short paragraphs or a tight bullet list.
  - Give the technical reason when asking for a change.
  - Comment on the highest-value issue, not every tiny preference.
  - Match thread language; in Spanish use Rioplatense/voseo (`podés`, `tenés`, `fijate`, `dale`).
  - No em dashes — use commas, periods, or parentheses instead.

## go-testing
- **Trigger**: Go tests, go test coverage, Bubbletea teatest, golden files.
- **Path**: `/Users/agustinprieto/.claude/skills/go-testing/SKILL.md`
- **Compact Rules**:
  - Use table-driven tests for pure functions and multiple cases.
  - Test Bubbletea Model state transitions directly via `Model.Update()`.
  - Use `teatest.NewTestModel()` for full TUI flows; use golden files for visual output.
  - Mock dependencies with interfaces; use `t.TempDir()` for file operations.
  - Skip integration tests with `go test -short`.
  - Update golden files with `go test -update`.
  - Keep test files adjacent to source (`foo_test.go` next to `foo.go`).

## issue-creation
- **Trigger**: Creating GitHub issues, bug reports, or feature requests.
- **Path**: `/Users/agustinprieto/.claude/skills/issue-creation/SKILL.md`
- **Compact Rules**:
  - Blank issues are disabled — MUST use a template (bug report or feature request).
  - Every issue gets `status:needs-review` automatically on creation.
  - A maintainer MUST add `status:approved` before any PR can be opened.
  - Questions go to Discussions, not issues.
  - Search existing issues for duplicates before creating.
  - Fill in ALL required fields and check pre-flight checkboxes.
  - Use `gh issue create --template` with conventional-commit-style titles.

## judgment-day
- **Trigger**: judgment day, dual review, adversarial review, juzgar.
- **Path**: `/Users/agustinprieto/.claude/skills/judgment-day/SKILL.md`
- **Compact Rules**:
  - Launch TWO independent blind judge sub-agents in parallel via `delegate`.
  - The orchestrator NEVER reviews code itself — only coordinates, synthesizes, and fixes.
  - Resolve skills first: read registry → match by code + task context → inject compact rules into BOTH judge and fix prompts.
  - Judges classify every WARNING as `real` (fix required) or `theoretical` (report as INFO, do not fix).
  - After 2 fix iterations, ASK the user before continuing; never escalate automatically.
  - Do NOT push, commit, or save summary until every JD reaches APPROVED or ESCALATED.
  - Confirmed findings → high confidence, fix immediately. Suspect findings → triage. Contradictions → flag for manual decision.

## work-unit-commits
- **Trigger**: implementation, commit splitting, chained PRs, or keeping tests and docs with code.
- **Path**: `/Users/agustinprieto/.config/opencode/skills/work-unit-commits/SKILL.md`
- **Compact Rules**:
  - A commit represents a deliverable behavior, fix, migration, or docs unit.
  - Do not commit by file type (`models`, then `services`, then `tests`).
  - Keep tests with the code they verify; keep docs with the user-visible change they explain.
  - Each commit should tell a story and be a candidate chained PR when the change grows.
  - If SDD tasks forecast >400 lines, group commits into chained PR slices before implementation.
  - Verify: one clear purpose, repo still makes sense after the commit alone, rollback is reasonable.
